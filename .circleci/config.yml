version: 2.1

orbs:
  heroku: circleci/heroku@1.2

jobs:
  build-and-test:  
    docker:
      - image: cimg/openjdk:8.0
      - image: cimg/node:13.12-browser
    steps:
      - browser-tools/install-chrome
      - browser-tools/install-chromedriver
      - checkout
      - run:
          name: Build
          command: mvn -B clean package -P pro

workflows:
  sample:
    jobs:
      - build-and-test
      - heroku/deploy-via-git:
          force: true
          requires:
            - build-and-test
          filters:
            branches:
              only: 
              - develop